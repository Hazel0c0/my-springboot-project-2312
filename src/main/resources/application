server:
  port: 9090

# MySQL
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bibi?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username:
    password:

  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

    properties:
      hibernate.format_sql: true